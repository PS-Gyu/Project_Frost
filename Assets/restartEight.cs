using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class restartEight : MonoBehaviour
{
    LightOffMuseum lom;
    // Start is called before the first frame update
    void Start()
    {
        lom.restartScene();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
